- # 项目:《转错了》Odd Rotation — H5 休闲游戏

  ## 一句话玩法

  一张图被切成若干网格,只有少数几块被旋转了。找出它们并转正。

  ## 产品结构(v2 核心变更)

  本项目有**两个模式**,分工明确,不可混淆:

  ### A. 街机模式(留存引擎,主玩法)

  - 连续流:一张图接一张图,不打断、无结算页
  - 每关独立倒计时,找对 → 补时 + 连击 +1;找错 → 扣时
  - 时间归零 → Game Own,展示连击数 + 纪录 + **单个"再来一局"按钮**
  - 本模式**恒定 1 个目标格,且点中即自动转正**(极简极快,不做旋转交互)
  - 难度随连击数动态上升(见下)
  - 存在意义:提供"再来一局"的动机 = 分数可以更高

  ### B. 关卡模式(50 关,内容与教学)

  - 逐关通过,有难度曲线与变奏节点
  - 后半段引入**多目标格 + 需要手动旋转**(有进度感"已修复 1/3")
  - 存在意义:教学、展示设计深度、向街机模式解锁内容

  ### 两模式的关系(必须遵守)

  - **首次进入游戏 → 强制先玩关卡模式第 1~3 关**,然后解锁街机模式

  - 关卡模式的进度

    解锁街机模式的内容池

    :

    | 关卡进度   | 解锁到街机模式                  |
    | ---------- | ------------------------------- |
    | 通过 3 关  | 街机模式开放                    |
    | 通过 20 关 | 六边形网格进入街机随机池        |
    | 通过 26 关 | 三角形网格进入池                |
    | 通过 39 关 | Voronoi 网格进入池              |
    | 通过 50 关 | 解锁"混沌模式"(全形态 + 微角度) |

  - **绝不让两个模式争抢玩家时间**:关卡模式是"解锁器",街机模式是"跑步机"。 关卡模式卡关时,首页要显眼地引导去街机模式;街机模式打出高分时,提示"通过第 20 关可解锁六边形"。

  ### C. 每日挑战(留存钩子,v1.0 必须有)

  - 用当天日期做 seed,全球同题
  - 固定 10 关,比总用时
  - 无后端:本地记录 + 生成一张成绩图供分享
  - 每天只能玩 1 次(可重试但记录首次)

  ## 不可协商的设计原则

  1. **3 秒上手**:街机模式图出现 → 点一下。关卡模式第 1 关无任何文字教学。
  2. **30 秒体会乐趣**:靠**连击**,不靠单次"找到"。连击音必须随连击数升调。
  3. **3 分钟想再来**:靠分数纪录 + 每日挑战 + 解锁进度。**不靠加难度。**
  4. 难度收敛在"找"。街机模式完全不做旋转交互;关卡模式后半段的旋转必须有 15° 内自动吸附。
  5. **绝不允许任何一关退化成"乱点碰运气"**。宁可降低难度上限。
  6. 不需要复杂美术和故事。视觉只做到"干净、反馈清楚"。

  ## 街机模式难度爬升(替代 v1 的固定曲线)

  难度由**当前连击数 c** 驱动,连续函数而非离散关卡:

  | 连击 c | 网格密度 | 图像方向特征档 | 旋转角度   | 单关时限 |
  | ------ | -------- | -------------- | ---------- | -------- |
  | 0–4    | 3×3      | strong         | 180°       | 8s       |
  | 5–9    | 4×4      | strong         | 180/90/270 | 7s       |
  | 10–17  | 4×4~5×5  | mid            | 全角度     | 6.5s     |
  | 18–27  | 5×5      | mid            | 全角度     | 6s       |
  | 28–40  | 5×5~6×6  | mid→weak       | 全角度     | 5.5s     |
  | 41+    | 6×6 上限 | weak           | 含微角度   | 5s(下限) |

  铁律:

  - **网格密度上限 6×6,时限下限 5s**。到顶后只靠图像难度和形态微调,不再加密。
  - 每 5 连击必须插入 1 关**明显容易**的(strong + 3×3),制造节奏喘息和"我还行"的错觉。
  - 所有街机关卡必须满足 `S > S_min`,与关卡模式同一门槛。

  ## 技术栈(固定,不要替换)

  - TypeScript + Vite
  - 渲染:**原生 Canvas 2D**,不引入 Phaser/PixiJS/Three
  - 唯一允许的运行时依赖:`d3-delaunay`(仅 Voronoi)
  - 无后端。localStorage 存档。Web Audio API 合成音效,不用音频文件
  - 纯 H5 网页,移动端竖屏单手可玩,静态部署
  - 首屏 < 500KB;中端安卓 60fps
  - **街机模式必须预加载下一张图**,切换零等待(这是连击流畅感的前提)

  ## 目录结构(严格遵守)

  ```
  /core      纯逻辑,零 DOM 依赖,浏览器与 Node 共用  
    rng.ts        mulberry32 seeded PRNG  
    grid.ts       网格顶点生成(square/hex/tri/voronoi)  
    salience.ts   违和度 S / 干扰度 C / 难度 D  
    difficulty.ts 街机模式:连击数 → 关卡参数  
    types.ts  
  /gen       Node CLI:批量打分 → 过滤 → 曲线编排 → levels.json  
  /game  
    /arcade      街机模式  
    /campaign    关卡模式  
    /daily       每日挑战  
    /shared      渲染器 / 输入 / tween / 音效 / 存档  
  /assets    AI 生成图,WebP,1:1,短边 1080px + manifest.json  
  /levels    levels.json  
  /snapshots 版本快照  
  /tests  
  ```

  ## 架构铁律(违反即致命 bug)

  - **`/core/grid.ts` 必须被生成器和渲染器共用。** 同 seed + 同参数,在 Node 和浏览器产出**完全相同的顶点数组和 cellId 顺序**。必须有一致性单测。
  - `/core` 禁止出现 `document`/`window`/`canvas`/`Image`。
  - 关卡数据是纯 JSON,不含逻辑。
  - **禁止裸用 `Math.random()`**,全部走 `/core/rng.ts`。
  - **渲染器必须模式无关**:`/game/shared/renderer.ts` 接收一个统一的 `PuzzleState`,不知道自己在服务哪个模式。街机/关卡/每日只是不同的**状态供给者**。

  ## 性能铁律

  每关首次渲染时,把所有**非目标格**一次性绘制到离屏 Canvas 缓存;每帧只重绘目标格。 帧成本必须与格子总数无关。街机模式还需维护一个双缓冲(当前关 + 预加载的下一关)。

  ## 关卡数据格式

  ```json
  {  
    "id": 39,  
    "difficulty": 4.6,  
    "image": "img/mosaic_07.webp",  
    "grid": { "type": "voronoi", "seed": 20481, "cells": 24 },  
    "targets": [ { "cellId": 13, "rotation": 137.5 } ],  
    "mode": { "autoSnap": false, "showTargetCount": true },  
    "limits": { "timeLimit": null, "hints": 1 },  
    "star": { "clicks": [1, 3, 6], "time": [8, 20, 40] },  
    "meta": { "S": 0.42, "C": 0.19, "V_image": 0.71 }  
  }  
  ```

  ## 难度模型

  - `S` 违和度(越大越好找)= 边界梯度断裂度 + λ·(1 − SSIM(旋转前后格内容))

  - `C` 干扰度(越大越难)= 若被旋转、违和度 > τ·S(target) 的其他格占比

  - ```
    D = α/Ŝ + β·Ĉ + γ·log(N_grid) + δ·F_shape + ε·A_θ
    ```

    - 初始权重 = 0.30, 0.30, 0.15, 0.15, 0.10
    - F_shape: square=0, hex=0.3, tri=0.5, voronoi=0.8
    - A_θ: 180°=0, 90°/270°=0.4, ±30°=1.0

  - **硬门槛:`S > S_min` 才允许入选,两个模式都适用。**

  ## 关卡模式 50 关曲线铁律

  1. 新机制首次出现的那一关,总难度必须降一档。
  2. 连续两关不得都 D ≥ 8;关口关(19/35/47)之后必有回落关。
  3. 相邻 5 关不得重复同一张图;每 10 关至少切换一次网格形态。
  4. 变奏节点固定:20 关六边形、26 关三角形、**32 关首现"多目标 + 手动旋转"**、39 关 Voronoi、45 关微角度。
  5. 前 10 关无任何 UI 文字。
  6. **第 32 关起启用"只告知数量、不标位置"的多目标玩法**,界面显示"已修复 1/3",转错正常格会使计数上升(可撤销)。

  ## 工作方式要求

  - 每完成一个阶段,先自检本文件铁律再交付。
  - `/core` 改动先写测试再写实现。
  - 每个可玩版本打 git tag,`CHANGELOG.md` 追加 `vX.Y — 改了什么 — 为什么`。
  - 有设计歧义**先问我**,不要自行发明规则。
  - 按阶段交付,每阶段结束等我确认,不要一次生成几千行。

  ## 已知的 AI 常犯错误(每次动手前自查)

  1. 擅自安装 Phaser / canvas 库 → 禁止,只允许 d3-delaunay
  2. 把网格生成写进渲染代码 → `/core` 纯净性是本项目第一大坑
  3. 使用 `Math.random()` → 全局禁用
  4. 手写 50 关硬编码 JSON → 关卡是生成器的**产物**,只有第 1~3 关允许手工精调
  5. 跳过测试 → rng/grid/salience/difficulty 四个模块必须 TDD,它们错了会静默污染所有关卡
  6. 把街机模式做成"关卡模式的无限版" → 两者交互形态不同,街机**没有旋转交互**